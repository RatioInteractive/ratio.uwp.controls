using System.Diagnostics;
using Windows.ApplicationModel;
using Windows.Foundation;

namespace Ratio.UWP.Controls
{
    public sealed class RowlistItem : BaseItem
    {
        public RowlistItem()
        {
            DefaultStyleKey = typeof(RowlistItem);
        }
    }
}
